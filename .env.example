# Google Apps Script web app deployment URL (ends in /exec)
# https://script.google.com/macros/s/.../exec
VITE_FORM_SUBMIT_URL=

# Optional: must match Script property SUBMIT_SECRET in Apps Script (mild abuse friction only; visible in client bundle)
# VITE_FORM_SUBMIT_SECRET=

# GitHub Actions: add repository secrets VITE_FORM_SUBMIT_URL and optional VITE_FORM_SUBMIT_SECRET
# (Settings → Secrets and variables → Actions) so the Pages deploy workflow can build with the form URL.

# Google Analytics 4 measurement ID (free — create at https://analytics.google.com/)
# Example: G-XXXXXXXXXX
# GitHub Actions: add secret VITE_GA_MEASUREMENT_ID to enable stats on production builds.
# VITE_GA_MEASUREMENT_ID=

# Private /insights dashboard (not linked from public nav; robots.txt Disallow)
# VITE_STATS_ACCESS_KEY=          # min 8 chars — bookmark https://theonlypm.com/insights
# VITE_LOOKER_STUDIO_EMBED_URL=   # Looker Studio → Share → Embed report URL (GA4 charts)
# VITE_STATS_SHEET_EMBED_URL=     # Google Sheet → Publish to web → embed URL (mailing list rows)

# SEO / GitHub Pages dual-URL build (set in CI; see .github/workflows/deploy.yml)
# CANONICAL_BASE=https://theonlypm.com
# SITE_BASE_PATH=/pm-website
